version: '3.8'
services:
  cbkt:
    build: .
    container_name: app
    ports:
      - "4000:4000"

  nginx:
    image: nginx
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "80:80"
    depends_on:
      - cbkt
